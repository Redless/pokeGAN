to do:
see if it's randomness or starting_config that produces good results
read more papers
cyclic learning rate
wasserstein loss function
"compute distance between generated vectors" as an additional loss function that encourages different inputs to lead to different outputs
